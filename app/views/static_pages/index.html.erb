<div class="row">
  <div class="col-md-6 col-md-offset-3">

<ul>
  <% if moussaillon_signed_in? %>
  <h3 class="text-center"><%= "Bienvenue #{current_moussaillon.username} ..."%></h3>
  <table class="table">
    <thead>
      <tr>
        <th>Les rumeurs qui circulent</th>
        <th>Auteur Anonyme</th>
        <th>Depuis</th>
        <th></th>
      </tr>
    </thead>
    <tbody>
      <% @gossips.each do |gossip| %>
        <tr>
          <td><%= gossip.content %></td>
          <td><%= gossip.moussaillon.username %></td>
          <td><%= gossip.created_at.strftime("%d-%m-%Y") %></td>
          <td>
            <% if current_moussaillon.email == gossip.moussaillon.email %> <!-- Ici on check si le gossip appartient au moussaillon loggé -->
            <%=link_to'Stopper la rumeur !', gossip_path(gossip), method: :delete %>
            <% end %>
          </td>
        </tr>
        <% end %>
    </tbody>
  </table>
  <% else %>
  <p class="text-center">Il semblerait que tu ne fasses pas encore parti des membres ou alors t'es pas connecté !!</p>
  <% end %>
</ul>
