<% provide(:title, 'Balance ton gossip !') %>
<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_for(@gossip) do |f| %>

      <%= f.label :content, "Alors c'est quoi ce gossip ;) ?" %>
      <%= f.text_field :content, class: 'form-control' %>

      <%= f.submit "Balance ton gossip !", class: "btn btn-primary" %>
    <% end %>
  </div>
</div>
